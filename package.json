{
  "name": "CSV-Regexp-searcher",
  "version": "1.0.0",
  "description": "Parses a CSV file and returns the elements containing the expressions passed as arguments",
  "main": "convert.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "csv-parser": "^2.3.3",
    "d3": "^5.16.0"
  },
  "author": "Guillaume Levrier",
  "license": "MIT"
}
